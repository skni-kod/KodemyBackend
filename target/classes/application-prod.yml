spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/kodemy
    username: kodemy_usr
    password: password
  liquibase:
    contexts: prod
  sql:
    init:
      platform: postgres