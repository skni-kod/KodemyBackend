server.port: 8081

spring:
  data:
    redis:
      host: localhost
  datasource:
    url: jdbc:postgresql://localhost:5435/kodemy?currentSchema=kodemy-auth
    username: postgres
    password: postgres
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: Ov23liqWT50Vtt0A0uIg
            clientSecret: 8d3b1c72e34f2077e433c46f0aea4b980c15226b

management:
  server:
    port: ${server.port}

logbook:
  obfuscate:
    headers:
      - Authorization

service:
  baseUrl:
    gateway: http://localhost:8080

jwt:
  bearer:
    expiration-min: 720

app:
  security:
    oauth2:
      baseUrl:
        front: http://localhost:3000
        gateway: ${service.baseUrl.gateway}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka